# CONFIGURACIÓN DE VARIABLES DE ENTORNO - MIPROFESIONAL BACKEND
# Copiar este contenido a tu archivo .env.local

# MongoDB Atlas - Configuración CRÍTICA
MONGODB_URI=mongodb+srv://miprofesional_luis:Luisaguerre1966@miprofesional-cluster.zhkc2iq.mongodb.net/miprofesional?retryWrites=true&w=majority

# JWT Secret - Configuración CRÍTICA
JWT_SECRET=MI_PROFESIONAL_JWT_SECRET_KEY_2024_SUPER_SEGURA_PRODUCTION_CHANGE_ME

# Entorno
NODE_ENV=development

# Puerto local
PORT=3001

# CORS Origins (opcional)
CORS_ORIGIN=http://localhost:3000,http://localhost:8081

# Rate Limiting (opcional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logs (opcional)
LOG_LEVEL=info
LOG_FILE=logs/app.log
